program try_it;

procedure all(n:integer; var x: real);

   function _try_me(i,j:integer): real;

      begin
      if (n <= 0) and a then
         v := integer;
        v := -1
      else
         
         v := -l
      end;

      
      begin
      for q := 1 to q[10,q[q+q]] do

      write("endofprogram");
   end;

   begin
      y := 1
end.